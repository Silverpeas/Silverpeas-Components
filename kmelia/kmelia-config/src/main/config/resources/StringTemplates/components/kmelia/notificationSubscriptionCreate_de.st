In einem Ihrer Abonnemente ist ein Dokument erstellt worden.
Name der Veröffentlichung : "$publicationName$"
Speicherort : $path$

$if(notification_receiver_users)$
Diese Nachricht wurde gesendet, um : $notification_receiver_users$
$endif$

$if(notification_receiver_groups)$
und Gruppen : $notification_receiver_groups$
$endif$